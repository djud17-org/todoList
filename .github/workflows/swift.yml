---
name: Swift
on:
  pull_request:
    branches:
      - develop
jobs:
  build:
    runs-on: "macos\0-latest"
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: "swift build \0-v"
      - name: Run tests
        run: "swift test \0-v"
