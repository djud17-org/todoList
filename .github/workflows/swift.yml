name: Swift
on:
  pull_request:
    branches:
      - develop
jobs:
  build:
    xcodebuild -project taskManager.xcodeproj -scheme taskManager -destination 'platform=iOS Simulator,name=iPhone 11’
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: swift build -v
      - name: Run tests
        run: swift test -v
